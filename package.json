{
  "name": "shtorm",
  "license": "GPL-3.0-or-later",
  "private": true,
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run --parallel build"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
