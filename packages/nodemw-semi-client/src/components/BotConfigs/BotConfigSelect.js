import React from 'react'
import { Typography } from '@material-ui/core'

export default function BotConfigSelect () {
  return (
    <div className='bot-config-select-container spotlight'>
      <Typography variant='h6'>Nothing to see here yet!</Typography>
      <Typography variant='subtitle1'>Select a config in the sidebar or create a new one.</Typography>
    </div>
  )
}
