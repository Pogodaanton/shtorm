import React from 'react'
import { Typography } from '@material-ui/core'

export default function UsersSelect () {
  return (
    <div className='users-select-container spotlight'>
      <Typography variant='h6'>Nothing to see here yet!</Typography>
      <Typography variant='subtitle1'>Select a user in the sidebar or add a new one.</Typography>
    </div>
  )
}
